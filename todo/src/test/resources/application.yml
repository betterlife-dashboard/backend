spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

rabbitmq:
  user-deleted:
    exchange: event.user.deleted.exchange
    queue: event.user.deleted.todo.queue
    key: event.user.deleted.key
  todo-deleted:
    exchange: event.user.deleted.exchange
    key: event.user.deleted.key
  todo-updated:
    exchange: event.todo.updated.exchange
    key: event.todo.updated.key
  todo-created:
    exchange: event.todo.created.exchange
    key: event.todo.created.key

logging:
  level:
    org.hibernate.sql: debug
