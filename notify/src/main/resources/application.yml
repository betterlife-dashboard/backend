server:
  port: 8083

spring:
  application:
    name: notify
  datasource:
    url: ${NOTIFY_DATASOURCE_URL}
    username: ${NOTIFY_DATASOURCE_USERNAME}
    password: ${NOTIFY_DATASOURCE_PASSWORD}
  rabbitmq:
    host: ${RABBIT_URL}
    port: ${RABBIT_PORT}
    username: ${RABBIT_USERNAME}
    password: ${RABBIT_PASSWORD}

rabbit:
  exchanges:
    auth: "auth.exchange"
    todo: "todo.exchange"
  deadline:
    queue:
      name: "todo.deadline.queue"
      key: "todo.deadline.key"
  reminder:
    queue:
      name: "todo.reminder.queue"
      key: "todo.reminder.key"

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: health,info